package core.basesyntax;

/**
 * <p>Реалізувати свою HashMap, а саме методи `put(K key, V value)` та `getValue()`. Дотриматися
 * основних вимог щодо реалізації мапи (initial capacity, load factor, resize...) За бажанням
 * можна реалізувати інші методи інтрефейсу Map.</p>
 */
public class MyHashMap<K, V> implements MyMap<K, V> {

    @Override
    public V put(K key, V value) {
        return null;
    }

    @Override
    public V getValue(K key) {
        return null;
    }
}
